# Cyberpunk 2077

# Enable DLSS override
enable_dlss_override

# Enable DLSS indicator
# enable_dlss_indicator

# Skip startup launcher and videos
command+=" --launcher-skip -skipStartScreen -modded"
