# Minecraft: Java Edition

# Enable LSFG frame generation
# PrismLauncher runs the game with `preferredGraphicsBackend:"vulkan"` in the instance
# options.txt, and the `Minecraft` profile matches the game JVM by its `java` executable
# name, which leaves the launcher process out.
unset DISABLE_LSFGVK
