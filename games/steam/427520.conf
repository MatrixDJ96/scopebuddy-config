# Factorio

# Resolve game paths
GAME_CONFIG_PATH="Factorio"

# Symlink user data to Proton roaming directory
symlink_directory \
  "${WINDOWS_ROAMING_PATH}/${GAME_CONFIG_PATH}" \
  "${PREFIX_ROAMING_PATH}/${GAME_CONFIG_PATH}"

# Symlink Linux user data to Windows roaming directory
symlink_directory \
  "${WINDOWS_ROAMING_PATH}/${GAME_CONFIG_PATH}" \
  "${HOME}/.factorio"
